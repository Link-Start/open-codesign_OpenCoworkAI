# Current research status

**WIP / NOT NATIVE READY / NOT PILOT ADMITTED.** This publication-adapted working snapshot is not an F0 byte freeze, F1 freeze, native calibration certificate or pilot execution permit. This status overrides older “ready”, “current”, “implemented” and passing-test wording in historical `plans/` and `public-contracts/` documents.

## Explicit version precedence

1. **Statistics:** only `src/analysis-v04-v2.mjs` is the current conditionally audited method implementation. `src/analysis-v04.mjs`/v1 inferential intervals were rejected by independent audit and are retained as historical mechanism code, not an endorsed alternative. A pure-test pass cannot rehabilitate v1 statistical qualification. V2 does not certify real data/assumptions or authorize inference.
2. **Protocol:** `plans/v1.1/protocol-amendment-01.md` overrides conflicting older `protocol-v04.md`/`readiness-v04.md` language. R keeps non-noop; G keeps original-health eligibility **without adding non-noop**. Ninety-six refers to independently sourced/selected briefs, **not 96 functional-family categories**. Actual lineage/cluster dependence remains a separate evidence gate.
3. **Measurement:** r2/F0 are legacy/preparation only. Latest r3 has no complete current-byte calibration. Historical `browser-calibration-r3-MDiU66` policy/report cannot be reused or hash-retagged for this revision/environment.
4. **Execution:** the public study and r2/r3 calibration entry points are hard-blocked by `PUBLICATION_NATIVE_EXECUTION_BLOCKED`; native runtime construction has `PUBLICATION_NATIVE_ADMISSION_BLOCKED`. Their single-argument `--check-only` safety reports are explicitly non-evidence. A shaped manifest cannot unlock them. All native study execution remains blocked; pure implementation validation is separate: the exported default-checkout suite passed 461 of 462 tests, with one explicit historical skip and no failures. This is not native or scientific sign-off.

## Current evidence and limits

| Area | What exists | What remains unsigned or unimplemented |
| --- | --- | --- |
| Four methods | FULL, SCOPE_OFF, PROVENANCE_OFF and injected LLM_ONESHOT implementations, same-candidate scope controls and visual-intent fairness code | No real native full-chain experiment or real LLM editing result. Stock does **not** silently translate `borderBoxDistance` into CSS `gap`; deterministic unsupported visual intents remain in the funnel. |
| Native evidence seal | `src/study-native-evidence-seal-v1.mjs` fail-closed boundary | **Only two async functions throwing `NATIVE_EVIDENCE_SEAL_IMPLEMENTATION_PENDING`.** Actual raw sealing, fixed independent verification and persist/normalize integration are not implemented. A filename or planned API is not implemented evidence. |
| Typed evaluation | A02/A03 and related typed fixes have regression controls | Typed receipt replay/hash preservation is not native leaf verification or native end-to-end proof. |
| Observer | Latest allow-forms, `containerPolicyId`, keyboard/style, raw identity and journal changes | Original latest revisions had only Node syntax checks; the export now also passes the documented pure/synthetic suite after updating test-only container metadata. Historical **27 native controls and 35 unit tests do not sign current bytes**. Real runner policy generation/container wiring and the complete current-byte native matrix remain unverified. |
| Native safety | Limited historical controls | Unprevented fragment and external-HTTPS form negative controls remain pending, including no navigation/outbound application request and preserved rejection diagnostics. |
| Final journals | Public pre/post teardown contract | Evaluation adapter (historical owner 508) pre-close/post-close final-native-journal producer is **not implemented**. Scope completeness, observation cutoffs, teardown distinction and capture-prefix/final consistency need producer/sealer/verifier integration. |
| Bridge | Composition and synthetic-acquisition controls | Genuine baseline → public preparation → accepted candidate → sealed raw receipt → independent normalization full-chain controls, current calibration and admission remain open. Publication native runtime construction is blocked pending rawseal/final-journal integration; this fail-closed guard is not native success. |
| G coverage | Latest aggregate: **121 facets = 36 covered + 46 partial + 39 missing** | Facets are requirement/assertion coverage units, **not samples or experiment outcomes**. Full G coverage and independent new-revision governance are unsigned. Private planning remains independently sealed and excluded. |
| Statistics v2 | Independent method/implementation audit conditionally passed under stated assumptions | Real source pool, lineage/clusters, execution independence, positive-denominator assumptions, external trust anchors and pre-outcome sign-off remain unsigned. Synthetic controls establish none of these facts. |
| Onlook | Build and 13 safety negative controls passed historically | Real no-edit roundtrip and editing are not established. Windows `CreateProcess` access-denial cause is **unknown**, not an Onlook editing/tool failure or ranking result. |

## Collection and authorization

- Natural generation attempts: **0**. Real LLM editing attempts: **0**.
- Conditional authorization: fixed 8-brief pilot, **16 one-shot generations and at most 64 one-shot LLM edits**, only after independent admission and coordinator trusted-chain sign-off; no retry, replacement or best-of.
- Formal 96-brief collection is **not authorized**. A target design is not an achieved dataset or proof of independence.
- Tests, trusted controls, facets, simulations, source slots and independent briefs are distinct units. Never substitute test counts for sample size.
- R/G are distinct eligibility masks on one proposal; do not add denominators. Accepted unknown remains `U_A`, not success or retroactive rejection. Refusal is not success.

## Priorities and sign-off boundaries

1. **P0: raw sealer + fixed independent verifier + actual final-journal producer**; wire exact-byte persistence/normalization and challenge missing/mutated leaves, identities, scopes, cutoffs and prefix consistency.
2. Complete native form/keyboard/style/journal negative controls and container-policy wiring. Review portable calibration runner paths and the complete input/output closure; only a separately authorized, reviewed implementation revision may enable controlled calibration after P0 evidence plumbing, not a flag or guard deletion. Freeze inputs and run full current calibration on the actual browser/runtime/compiler, then obtain independent review before enabling native study execution. Never reuse MDiU66 or paste old hash labels onto a new environment.
3. Establish the real admitted bridge on public handwritten controls with real acquisitions, durable pre-evaluation locks and independently reread evidence. No natural/model dispatch by default.
4. Complete G requirement-to-assertion coverage and private independent governance without exposing expectations to method authors; issue new revisions, preserving historical seals.
5. Obtain a cleared source pool, real dependence/cluster evidence and statistical assumptions/signatures before inference, plus exact-revision execution approval.

## Product and publication boundary

Product #430 was merged into upstream `main` **by upstream, not by this research-publication agent**. This publication adds the research directory to the main baseline, not new product changes. It does not create a PR or merge. Source WIP remains unchanged; publication adaptations are separate in [export notes](publication/DOCS-EXPORT-NOTES.md). Final source/export hashes and validation receipts belong to the coordinator's manifest.

No current-byte native readiness, pilot admission, F1 freeze, source-pool certification or real-assumption signature is granted here.
